const Discord = require('discord.js');
const client = new Discord.Cliend();

cliend.on('ready', () => {
	console.log('I am ready!');
]);

client.on('message, message => {
	if (message.content === ping') {
	  message.reply('pong')};
	}
});

client.login(process.enc.nxTZAnLA1kV87v0cnaN6EL9ghumM23W3);
